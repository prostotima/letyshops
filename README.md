

## 📍 1. Структура сайту (Routes)
У проекті реалізована навігація між наступними сторінками:

| Файл / Шлях | Опис сторінки |
|---|---|
| **`/index.html`** (Home) | Головна сторінка. Містить банер, пошук, галерею, список магазинів та блок з API. |
| **`/login.html`** | Сторінка авторизації. Містить форму входу з валідацією (RegEx) логіна, пошти та пароля. |
| **`/oops.html`** | Сторінка-заглушка. Відображається, якщо користувач натискає "Більше магазинів". |
| **`/about.html`** | Інформаційна сторінка (посилання з футера). |

---

## 🌐 2. API Endpoints (Зовнішні API)
Сайт не має власного бекенду, але використовує асинхронні запити (`fetch`, `async/await`) до публічних API:

| HTTP Метод | URL Ендпоінту | Призначення у проекті |
|---|---|---|
| **GET** | `https://api.apis.guru/v2/list.json` | **Каталог API.** Завантажує список популярних API, їх логотипи та назви для відображення на головній сторінці. |
| **GET** | `https://picsum.photos/200/300` | **Галерея.** Використовується для генерації випадкових зображень у блоці "Галерея" (завантаження із затримкою). |

---

## 🛠 Технології
* **HTML5** (Семантична верстка)
* **CSS3** (Flexbox, Grid, адаптивність)
* **JavaScript (ES6+)**
  * DOM Manipulation
  * Events (click, hover, input)
  * Async/Await & Fetch API
  * RegExp (Валідація форм)
  * LocalStorage / Set (Робота з даними)